lb config -a i386 --debian-installer live

Added apt-key to /config/archives

Added sources.list entries in config/archives for backports and cbpp repos [build and live]

Package lists at config/package-lists/*

lb config --mirror-binary http://http.debian.net/debian/ --mirror-binary-security http://security.debian.org/ 

** broken?
  --mirror-binary-backports http://ftp.debian.org/debian/

lb config --apt-indices false

lb config --security true --updates true --backports true

lb config -k 586

Add cbpp-metpackage to config/package-lists/*

lb config --bootappend-live "boot=live components username=live"

Added lightdm greeter conf file to includes
